const useStats = () => {
  const statTitles: string[] = [
    "Recent collect activity",
    "Recent comment activity",
    "Recent mirror activity",
    "Recent react activity",
    "Recent collect activity",
    "Recent comment activity",
    "Recent mirror activity",
    "Recent react activity",
  ];

  return {statTitles}
};

export default useStats;